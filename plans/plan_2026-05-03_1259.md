---
created: 2026-05-03T12:59:39.662Z
session: f488bca5-3e56-481f-84ea-9dcb3c3193c0
working_directory: C:\Development\New Extension Build
---

## Context

I explored all 6 Excel report files and cross-referenced them against the current app's data arrays. The reports contain real production data spanning 2024-2026 with ~50 underwriters across ~25 branches. I identified 8 underwriters with the richest cross-report data, each from a different branch:

| User | Full Name | Branch | ARRs | Bonds | LOAs | FS Accounts |
|------|-----------|--------|------|-------|------|-------------|
| dhellmann | Doug Hellmann | KENTUCKY | 21 | 693 | 16 | 45 |
| jliggett | Jake Liggett | CINCINNATI | 2 | 551 | 18 | 35 |
| wwilson | Wade Wilson | FLORIDA/ORANGE | 94 | 392 | 46 | 14 |
| kwittler | Kathy Wittler | ORANGE | 10 | 436 | 31 | 20 |
| jwhipkey | Jonathon Whipkey | TENNESSEE | 24 | 338 | 10 | 17 |
| akveton | Adam Kveton | CHICAGO CONTRACT | 20 | 313 | 13 | 14 |
| pholland | Patrick Holland | CAROLINA | 38 | 268 | 9 | 10 |
| dbossen | Doug Bossen | TWIN CITIES | 28 | 220 | 17 | 8 |

All 8 branches are confirmed to have data in Red Flag Audit (branch-level sheets) and Active Agencies. The current app has ~500KB in `app.js` with data and logic interleaved.

### Key architectural decisions
- **Separate data file:** Create `data.js` loaded before `app.js`. Contains all real extracted data + user profiles.
- **Real current date:** Remove the `BondBoxClock.setOverride()` demo date. Real data spans 2024-2026, aligns with today.
- **Per-branch bond subset:** Only load bonds for the 8 selected branches (~2-5K bonds instead of 25K).
- **User switcher in sidebar:** Replace the static user info with a dropdown that triggers full re-render.

---

## Implementation Steps

### Step 1: Create a Python extraction script (`extract_data.py`)

Write a Python script that reads all 6 Excel files and outputs `data.js` — a JavaScript file exporting all data arrays as global variables. The script will:

- **Account Review Rating Report** → `realARRs[]` — Extract all 921 rows. Map columns: Branch (strip code prefix), Account Name, Account #, FS Type, FS Date, Review Type, Review State, ARR Created Date, Reviewer (username), ARR Approval Date, Review Grade.
- **BondBox All Bonds** → `realBonds[]` — Filter to the 8 branches only. Deduplicate by Bond Number (keep latest row). Map: Bond Number, Created, Effective, Expiration, Agency, Branch, Underwriter, Bond Class.
- **Line of Authority Report** → `realLOAData[]` — All 849 rows. Map: Branch, Account Name, Account Number, Customer Number, Status, Expiration Date, Aggregate, Single, User.
- **Red Flag Audit Report** → `realRedFlagData{}` — Read all 24 branch sheets. Structure as `{ branchName: [{ account, fs, fye, zScore, netQuick, netWorth, debtEquity, ... }] }`.
- **Timeliness of Financial Statements** → `realFinancialTimeliness[]` — Detail sheet (835 rows). Map: Branch, Account Name, Account Number, Underwriter (full name), Profitability Status, 2025 NI, 2024 NI.
- **Active Agencies** → `realAgencies[]` — All 2300 rows. Map: Branch, Agency Name, Agency Code, commission rates, Status.
- **User profiles** → `USER_PROFILES[]` — The 8 selected underwriters with: username, fullName, role (all "Underwriter"), branch, avatar (initials).
- **Username-to-fullname map** → `USERNAME_MAP{}` — Built from Timeliness report cross-referenced with other files.

### Step 2: Create `data.js` by running the extraction script

Run the Python script to generate `data.js`. This file will be ~300-600KB of pure data declarations. Load it via `<script src="data.js">` before `app.js` in `index.html`.

### Step 3: Refactor `app.js` — Replace dummy data arrays with real data

Replace the 15+ `sampleXxx` arrays at the top of `app.js` with references to the real data from `data.js`. Specifically:

- `sampleARRs` → Filter `realARRs` by assignee matching current user's username
- `sampleBonds` → Filter `realBonds` by branch matching current user's branch
- `sampleLOAData` → Filter `realLOAData` by user matching current user's username
- `sampleRedFlagData` → Key into `realRedFlagData[currentUser.branch]`
- `sampleMyAccounts` → Derived from unique accounts across the user's ARRs + LOAs + Bonds
- `sampleFinancials` → Derived from `realFinancialTimeliness` filtered by user/branch

Data arrays without a report source (bid log, reminders, visitations, WIP, claims, conversations, notes, bond requests, premium AR) will keep synthetic/template data that's generated per-user with realistic variety. These will be created once per user switch using small generator functions.

### Step 4: Refactor `currentUser` and add user switching

- Change `const currentUser` to `let currentUser`
- Create `switchUser(username)` function that:
  1. Sets `currentUser` from `USER_PROFILES`
  2. Rebuilds all filtered data arrays (`sampleARRs`, `sampleBonds`, etc.)
  3. Updates sidebar user info display
  4. Updates dashboard greeting
  5. Calls all render functions (reuse the `initSteps` pattern from DOMContentLoaded)
  6. Navigates to dashboard

### Step 5: Add user switcher UI

- In `index.html`: Replace the static sidebar footer user info with a clickable element that opens a user picker
- Options: Dropdown or modal showing all 8 user profiles with name, branch, and role
- On selection: Call `switchUser(username)` 
- Update the hardcoded "Welcome, Jake" H1 to be dynamic (already has `id="dashboard-greeting"`)

### Step 6: Remove BondBoxClock demo override

- Remove or comment out `BondBoxClock.setOverride(new Date(2024, 3, 15, 9, 0, 0))` so the app uses real current date
- Update the demo/live banner logic accordingly — the banner becomes a simple "Prototype" indicator
- LOA expiration logic, action items, etc. will all compute against today's real date, which means some LOAs will show as expiring soon based on real expiration dates (mostly 2026-2027)

### Step 7: Adapt render functions for real data shapes

Minor adjustments to render functions where real data column names or formats differ from the current dummy data:
- Bond status: Real data doesn't have "Expiring Soon" — compute it from expiration date vs today
- ARR status: Map "Initial Review", "Supervisor Review", "Additional Review", "Complete" to the display statuses
- Grade display: Real grades use same A+/A/A-/B+/B/B-/C+/C scale — no change needed
- Date formats: Normalize all to consistent `MM/DD/YYYY` during extraction
- LOA amounts: Convert from comma-separated strings to numbers during extraction

### Step 8: Verify and test

- Syntax check `data.js` and `app.js` with Node
- Open app in browser, verify default user loads with real data
- Switch between all 8 users, verify:
  - Dashboard KPIs show correct counts for each user
  - Account Review table shows that user's ARRs
  - LOA view shows that user's LOAs with correct amounts
  - Bonds table shows bonds from that user's branch
  - Red Flag page shows that user's branch data
  - My Accounts page shows accounts derived from that user's data
- Commit and push to GitHub Pages

---

## Verification

1. `node -c data.js` — syntax check data file
2. `node -c app.js` — syntax check logic file
3. Manual browser testing: switch between all 8 users, verify each view renders data, KPI counts match detail pages
4. Check that LOA expiration dates produce correct "expiring within 30 days" counts against today
5. Verify no JavaScript console errors during user switching

---

## Critical Files

- `data.js` (NEW) — All real data extracted from Excel reports, ~300-600KB
- `app.js:44-600` — Data arrays section to refactor (replace dummy with real data references)
- `app.js:716` — `currentUser` declaration (change from const to let, add switchUser function)
- `index.html:111-119` — Sidebar footer user info (add switcher UI)
- `extract_data.py` (NEW, temporary) — Python script to transform Excel → JS data