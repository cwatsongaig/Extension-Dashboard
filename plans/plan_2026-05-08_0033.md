---
created: 2026-05-08T00:33:49.147Z
session: 70622b8e-f7b5-4e49-a54b-de79b6a3d1fb
working_directory: C:\Development\New Extension Build
---

## Context

I explored the full data pipeline: SQL queries in `api/queries/`, the FastAPI endpoints in `api/main.py`, the frontend API layer (`BondBoxAPI.loadDashboardData`), and the data mapper (`buildUserDataFromAPI`). 

**What can be wired up with existing infrastructure:**

1. **Bid Log** — SQL query, API endpoint, and frontend renderer all exist. Just need to: fetch it in `loadDashboardData`, and map it in `buildUserDataFromAPI`.
2. **Service Activity** — SQL query, API endpoint exist. The `buildUserDataFromAPI` has a stub. The frontend reads from `realServiceActivity` global. Just need to override that global with API data.

**What CANNOT be wired up (no database tables/queries exist):**
- Bond Requests, WIP, Claims, Visitations, Reminders, Conversations, Account Notes — these have no SQL source and no corresponding DB tables.

**Partially-fake fields that can be improved:**
- Bond `amount` — the DB schema doesn't have a penal sum / bond amount column in the current query's tables, so this stays random.
- LOA `used` — no "utilization" column exists in the DB, stays random.
- Premium AR aging buckets — the `agencies.sql` query only returns agency name/status. No premium/AR aging tables are queried. Stays random unless a new query is written (which requires knowing the DB schema for AR aging).

---

## Implementation Steps

### Step 1: Wire up Bid Log in `loadDashboardData` (app.js ~line 84)

Add a bid log fetch to the parallel fetches array. Since bid log requires an `accountId`, and we don't have one at dashboard-load time, we'll fetch it for the first account once accounts are loaded (or skip if no accounts). Alternatively, we can add a new bulk bid-log endpoint.

**Simpler approach:** Add a new `/api/data/bid-log-branch` endpoint + SQL query that fetches bid logs by branch (not per-account), so it can load alongside everything else.

### Step 2: Create `bid_log_branch.sql` query

A modified version of `bid_log.sql` that filters by branch instead of account ID.

### Step 3: Add `/api/data/bid-log-branch` endpoint in `api/main.py`

Following the existing pattern (branch parameter, `_safe_query`).

### Step 4: Add bid log fetch to `loadDashboardData` in `app.js`

Add `fetchJSON('/api/data/bid-log-branch', { branch: branchId })` to the parallel fetches.

### Step 5: Map bid log data in `buildUserDataFromAPI` (app.js ~line 787)

Map `liveData.bidLog` → `sampleBidLog` using the column names from the SQL query (`BidDate`, `ProjectName`, `Obligee`, `ContractValue`, `Warranty`, `BidBondAmount`, etc.) → the frontend field names (`bidDate`, `projectName`, `obligee`, `contractValue`, etc.).

### Step 6: Wire up Service Activity in `buildUserDataFromAPI` (app.js ~line 782)

Replace the empty stub with code that overrides the global `realServiceActivity` array with the API response data, mapped to the expected shape (`{ branch, code, user, action, jan, feb, ..., ytd }`).

---

## Verification

1. Start the app (`python -m uvicorn api.main:app --reload --port 8000`)
2. Open browser, check the Bid Log view — should show real data from DB instead of synthetic entries
3. Check Service Activity view — should show live data matching current year
4. Verify the data source indicator shows "LIVE"
5. Switch users and confirm bid log updates per branch
6. Test with API unavailable (stop server) — confirm graceful fallback to static data

## Critical Files

- `C:\Development\New Extension Build\app.js` — Frontend: `loadDashboardData` and `buildUserDataFromAPI` need mapping code
- `C:\Development\New Extension Build\api\main.py` — New endpoint for branch-level bid log
- `C:\Development\New Extension Build\api\queries\bid_log.sql` — Existing per-account query (reference for new branch query)
- `C:\Development\New Extension Build\api\queries\service.sql` — Existing service activity query (already works, just needs frontend wiring)

---

## User Context

in addition, I would like to add more users.  Like before, get their information from the org chart file to determine if contract or commercial and use necessary query to determine their branch access.