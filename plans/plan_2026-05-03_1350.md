---
created: 2026-05-03T13:50:53.392Z
session: 812df880-1443-494b-94b1-7d00e6aac658
working_directory: C:\Development\New Extension Build
---

## Context

The dashboard customization modal (`openDashboardSettings`) currently shows all panels in one flat list. There's already a placeholder "Manager Settings" section (line 908) that says "coming soon." The `isManagerRole()` function checks `MANAGER_ROLES` array and `chainTitles`. Admin users are defined in `ADMIN_USERS = ['Ken Bearley', 'Chris Watson', 'Geoff Smith']`.

Currently only Jake Miller (Branch Manager) would pass `isManagerRole()`. The other 8 profiles are all "Underwriter" or "Sr. Underwriter" roles. Chris Watson and Geoff Smith aren't in `USER_PROFILES` yet.

### What we're building:
1. **Manager Widgets** — a new category of dashboard panels (starting with "Service & Activity Summary") that are separated from the standard panels in the Customize Dashboard modal
2. **Role-based visibility** — Managers see both standard + manager panels. Regular underwriters see only standard panels. Admins see everything plus an assignment UI.
3. **Admin user profiles** — Chris Watson and Geoff Smith added as switchable admin users who can see all branches and all widgets
4. **Admin assignment panel** — In the Customize modal, admins see a section to grant manager widget access to specific users (stored in localStorage)

---

## Implementation Steps

### Step 1: Add Chris Watson and Geoff Smith to USER_PROFILES in data.js
- Chris Watson: Admin role, no specific branch (sees all)
- Geoff Smith: Admin role, no specific branch (sees all)
- Add them to USERNAME_MAP, chainTitles, sampleTerritories (they see all territories via `canSeeAllTerritories()`)

### Step 2: Define manager widget panels in DASHBOARD_DEFAULTS
Add a new `managerPanels` array to `DASHBOARD_DEFAULTS`:
```javascript
managerPanels: [
    { id: 'mgr-service-activity', label: 'Service & Activity Summary', visible: true, order: 0 }
]
```

### Step 3: Add the manager widget to WIDGET_REGISTRY
Register `'mgr-service-activity'` — a compact dashboard widget that shows a summary table of the Service & Activity data for the current user's branch (top users by YTD volume, pulled from `realServiceActivity`).

### Step 4: Add `isAdminUser()` helper
```javascript
function isAdminUser() {
    return ADMIN_USERS.includes(currentUser.name);
}
```

### Step 5: Update `openDashboardSettings` to split panels into sections
Replace the single flat panel list with:
- **Standard Panels** section — all existing panels (daily briefing, KPIs, exposure gauge, etc.)
- **Manager Reports** section — only shown if `isManagerRole() || isAdminUser()`. Shows manager-specific widget toggles.
- **Admin: Widget Assignment** section — only shown if `isAdminUser()`. Shows a list of all user profiles with checkboxes to grant them manager widget access. Stored in `localStorage` under `bondbox-manager-access`.

### Step 6: Update `getDashboardPrefs` / `saveDashboardPrefs`
Extend to include `managerPanels` alongside `panels`. The `renderDashboardLayout` function will render manager panels after standard panels if the user has access (is manager, is admin, or has been granted access by an admin).

### Step 7: Update `buildUserData` for admin users
Admin users (Chris Watson, Geoff Smith) have no username in the report data. Their `buildUserData` should show aggregate data across all branches rather than filtering to one user.

### Step 8: Handle the "granted access" check
```javascript
function hasManagerAccess() {
    if (isManagerRole() || isAdminUser()) return true;
    try {
        const granted = JSON.parse(localStorage.getItem('bondbox-manager-access') || '[]');
        return granted.includes(currentUser.username);
    } catch(e) { return false; }
}
```

---

## Verification
- Switch to Jake Miller (Branch Manager) — should see Manager Reports section in Customize, and the Service & Activity widget on dashboard
- Switch to Doug Hellmann (Underwriter) — should NOT see Manager Reports section unless granted by admin
- Switch to Chris Watson (Admin) — should see everything plus the Widget Assignment panel
- Use Chris Watson to grant Doug Hellmann manager access → switch to Doug → verify he now sees manager widgets
- Syntax check both files with Node

## Critical Files
- `app.js:505-560` — DASHBOARD_DEFAULTS, getDashboardPrefs, saveDashboardPrefs (extend with managerPanels)
- `app.js:860-920` — openDashboardSettings (split into standard/manager/admin sections)
- `app.js:566-750` — WIDGET_REGISTRY (add mgr-service-activity widget)
- `data.js:8-72` — USER_PROFILES (add Chris Watson, Geoff Smith)
- `app.js:6860-6890` — switchUser (handle admin users with no report data)